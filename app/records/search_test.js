'use strict';

describe('myApp.recordsSearch module', function() {
  beforeEach(module('myApp.recordsSearch'));

  describe('recordsSearch controller', function(){
    it('should ....', inject(function($controller) {
      //spec body
      var searchCtrl = $controller('RecordsSearchCtrl');
      expect(searchCtrl).toBeDefined();
    }));
  });
});